<%= @user.username %>

<%= link_to "Create New Goal", new_goal_url %>

<br>
<ul>
<% @valid_goals.each do |goal| %>
  <li> <%=link_to goal.content, edit_goal_url(goal)%> <br>
       <%= goal.privated ? "Private" : "Public" %>
       <%= goal.completed ? "Finished" : "Incomplete" %></li>
<% end %>
</ul>
